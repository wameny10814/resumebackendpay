const host = 'http://localhost:3500';
const config = {
    AB_LIST: `${host}/address-book/api/list`,
    MYFORM_API: `${host}/admin2/myform`,
    IMG_PATH: `${host}/img`,

};
export default config;


